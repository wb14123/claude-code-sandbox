#!/bin/sh

set -e

docker run -it \
  -v ~/.claude:/home/node/.claude \
  -v ~/.claude.json:/home/node/.claude.json \
  -v "$(pwd)":"$(pwd)" \
  -w "$(pwd)" \
  -u "$(id -u):$(id -g)" \
  wb14123/claude-code:latest \
  claude "$@"
